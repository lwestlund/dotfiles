#!/usr/bin/env bash

window_id=$(wmctrl -lx | grep scratch.Alacritty | awk '{print $1}')
if [[ -n $window_id ]]; then
    bspc node $window_id --flag hidden --focus
else
    # Spawn a scratch terminal
    alacritty \
        --class "scratch" \
        --command $SHELL \
        &
fi
